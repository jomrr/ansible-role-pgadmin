[project]
name = "pgadmin"
version = "0.0.0"
description = "Install pgAdmin 4 application packages and manage system-wide settings for web deployment."
requires-python = ">=3.12"
authors = [
  { name = "Jonas Mauer" },
]
license = "MIT"
keywords = ["postgresql", "pgadmin", "database"]

[project.urls]
Repository = "https://github.com/jomrr/ansible-role-pgadmin"

[tool.uv]
package = false

[dependency-groups]
ansible = [
  "ansible-core==2.20.*",
  "ansible-lint==26.*",
]

podman = [
  "molecule==26.*",
  "molecule-plugins[podman]==25.*",
]

[tool.commitizen]
name = "cz_conventional_commits"

[tool.semantic_release]
branch = "main"
commit_message = "build(release): version {version}"
major_on_zero = true
upload_to_pypi = false
upload_to_vcs_release = true
tag_format = "v{version}"
version_toml = [
    "pyproject.toml:project.version",
]
